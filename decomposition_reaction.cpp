#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define endl '\n'
#define fastIO cin.tie(0)->sync_with_stdio(0)

int main() {
	fastIO;
	int TC = 1;
	// cin >> TC;
	while (TC--) {
		int n, m;
		cin >> n >> m;
		vector<int> q(n);
		for (int i = 0; i < n; ++i) {
			cin >> q[i];
		}
		vector<int> c(m), x(m);
		vector<pair<int, int>> wc;
		for (int i = 0; i < m; ++i) {
			cin >> c[i] >> x[i];
			int wi, ci;

			wc.emplace_back()
		}
	}
	return 0;
}
